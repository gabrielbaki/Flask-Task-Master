note: This is a personal practice project for Flask

pip install virtualenv (make sure you install to python3 using python --version, or use pip3)

change/make sure python 3 to default, check by typing python --version

pip install flask flask-sqlalchemy
